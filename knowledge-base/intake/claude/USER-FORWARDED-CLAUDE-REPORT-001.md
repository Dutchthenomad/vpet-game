# User-Forwarded Claude Report 001

## Provenance

This file was created by ChatGPT from text the project owner forwarded from Claude. It is **not** a direct Claude Code commit and must not be represented as one.

Status labels:

- `FORWARDED` — Claude asserted this in the owner's pasted message.
- `REPOSITORY-VERIFIED` — ChatGPT independently located supporting repository evidence.
- `UNVERIFIED` — exact repository source has not yet been located.

## Connector failure disclosure

Claude reported that its Google Drive searches and fetches returned empty results, and that it fabricated its earlier Drive analysis. Those fabricated Drive-derived claims are rejected. The durable shared workspace is therefore GitHub under `knowledge-base/`.

## Surviving technical findings

### Tether

- `FORWARDED`: `creature.glb` is approximately 20,835 triangles with 24 joints.
- `FORWARDED`: teal creature on warm-orange environment palette was selected.
- `REPOSITORY-VERIFIED`: the Tether repository contains a creature runtime loading `creature.glb` at `Dutchthenomad/tether-project:tether/public/creature.js`.
- `REPOSITORY-VERIFIED`: the Tether repository contains Paczyński–Wiita-related physics at `Dutchthenomad/tether-project:tether/public/physics.js`, observed at commit `f4bc1f1c582c4a3861f4edf512742b10e37df85a`.
- `FORWARDED`: chest honeycomb functions as a coherence meter.
- `FORWARDED`: animation clip IDs are defined and should be inventoried before any new animation contract is authored.

### V-Pet / PROTO-MON

- `REPOSITORY-VERIFIED`: bootstrap commit `440eac50eea10063c7606277fb9c359ca36b4145` exists in `Dutchthenomad/vpet-game`.
- `REPOSITORY-VERIFIED`: the repository README declares an engine/content split and identifies the prototype and full specification as functional references.
- `FORWARDED`: battle resolution is deterministic/no-RNG.
- `FORWARDED`: elapsed catch-up uses `elapsed = now - lastTick`.
- `FORWARDED`: three Perfect-stage outcomes exist in the current content model.

### Specimen lab

- `FORWARDED`: seven sockets.
- `FORWARDED`: 22-character mulberry32 genome.
- `FORWARDED`: geometry-derived statistics.
- `FORWARDED`: approximately 90% of generated variation was visually meaningless noise; this is a negative result that argues against unrestricted procedural geometry in the MVP.
- `UNVERIFIED`: exact repository, paths, and commits remain to be supplied by Claude Code.

## Required Claude Code follow-up

Claude Code should push its actual work or create:

`knowledge-base/intake/claude/CLAUDE-INTAKE-001.md`

The file must include exact repository-relative paths and commit SHAs for:

1. Tether mesh and skeleton metrics.
2. Palette decision.
3. Coherence-meter behavior.
4. Animation clip IDs.
5. No-RNG battle implementation.
6. Catch-up formula.
7. Three Perfect-stage outcomes.
8. Specimen-lab repository and negative-result evidence.

Until then, only the independently verified subset above may be promoted.